# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name='cpe-mml-syntax-checker',
    version='0.0.1',
    description="MSoft collaborative programming exercise: A simple MML syntax checker",
    license='UNLICENSED',
    packages=find_packages(exclude=('tests'))
)
